import { OcultarcontrasenaPipe } from './ocultarcontrasena.pipe';

describe('OcultarcontrasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarcontrasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
